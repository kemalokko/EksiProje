@using dnm.Models
@{
    ViewBag.Title = "Test";
}

<h2>Test</h2>

@{
    if (TempData["Message"] != null)
    {
        <p>@TempData["Message"].ToString()</p>
    }
    var user = Session["LoginModels"] as LoginModels;
}
<h3>welkam @user.Kek</h3>